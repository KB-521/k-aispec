# 前端架构概览

本文件用于记录前端应用边界、目录职责、状态流和公共能力。

## 状态

当前仓库无前端应用运行时。

## 已验证事实

- 当前仓库没有 `src/pages`、`src/components`、打包前端页面或浏览器端路由。
- `openspec/skills/web/*` 记录的是可分发的前端实现 workflow，不是当前仓库自己的前端应用。

## 影响

- 前端架构事实需要在真正的 Web 项目仓库中初始化；当前仓库只维护相关 workflow 模板与规则。
