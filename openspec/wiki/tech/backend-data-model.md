# 后端数据模型基线

本文件用于记录 Python 后端的数据模型、实体边界与迁移口径。

## 状态

当前仓库无独立后端持久化模型。

## 已验证事实

- 仓库内的 Python 相关内容是 OpenSpec 默认规则和技能模板，不对应当前仓库自己的数据库或 ORM 模型。
- 当前仓库没有 `pyproject.toml`、数据库迁移目录或运行中的 Python 服务实现。

## 影响

- 若后续引入真实后端模块，应在该模块落地后再补实体、枚举、迁移口径和兼容策略。
